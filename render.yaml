services:
  - type: web
    name: betadmin
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Redirects for SPA routing (if using static site, uncomment below and use static site type)
    # redirects:
    #   - source: /*
    #     destination: /index.html
    #     action: rewrite

